# minarch-cores Configuration

# Build options
JOBS=8                      # Parallel build jobs (default: 8)
                            # Adjust for your system if needed

# Build fix cores - cores with upstream build bugs we fix (patches/build/)
# These patches fix cross-compilation issues and apply to ALL builds
BUILD_FIX_CORES=snes9x2005 crocods fmsx hatari remotejoy stella2014

# Custom behavior cores - cores with minarch-specific modifications (patches/custom/)
# These will be built separately as -custom variants
CUSTOM_CORES=gambatte

# Recipe system handles all core selection and build flags
# See: libretro-super/recipes/linux/cores-linux-{arm7neonhf,aarch64}
# - armv7: 134 YES cores with ARM_NEON=1, CORTEX_A7=1, ARM_HARDFLOAT=1
# - aarch64: 137 YES cores (includes mupen64plus_next with dynarec)
